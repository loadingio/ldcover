// Generated by LiveScript 1.3.1
var slice$ = [].slice;
(function(){
  var ldcv, fire;
  ldcv = new ldCover({
    root: '.ldcv'
  });
  fire = function(){
    var args, dom, hash, opt;
    args = slice$.call(arguments);
    dom = Array.from(ldcv.root.querySelectorAll('[data-name]'));
    hash = {};
    dom.map(function(d){
      var name;
      name = d.getAttribute('data-name');
      return hash[name] = d;
    });
    if (typeof args[0] === 'object') {
      opt = args[0];
    } else {
      opt = {
        title: args[0],
        desc: args[1],
        type: args[2]
      };
    }
    hash["title"].innerHTML = opt.title;
    hash["desc"].innerHTML = opt.desc;
    hash["icon"].setAttribute('src', "assets/img/" + opt.type + ".svg");
    hash["cancel"].style.display = "none";
    return ldcv.get();
  };
  return fire('failed', 'your last action is not successful.', 'error');
})();